package telegram

import "jarvis/logger"

func (b *bot) Shutdown() {
	logger.App().Infof("======================================================================= TGBot start shutdown =======================================================================")
	defer logger.App().Infof("======================================================================= TGBot finish shutdown =======================================================================")

	close(b.close)

	<-b.done
}
